node_modules/

# Android build outputs and machine-specific files — regenerated by
# `npm install && npx cap sync android` and the Gradle build itself.
android/local.properties
android/.gradle/
android/build/
android/app/build/
android/captures/
android/.idea/
android/*.iml
android/app/*.iml
